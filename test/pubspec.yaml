name: fhir_path_test
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # fhir: ^0.11.4
  fhir:
    path: ../../fhir
  fhir_path:
    path: ../../fhir_path
  flutter:
    sdk: flutter
  ucum:
    path: ../../ucum

dev_dependencies:
  flutter_lints: ^2.0.2
  test: ^1.24.3


  
  