name: fhir_path_test
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  fhir_primitives:
    path: ../../fhir_primitives
  fhir_r4:
    path: ../../fhir_r4
  fhir_path:
    path: ../../fhir_path
  flutter:
    sdk: flutter
  ucum:
    path: ../../ucum

dev_dependencies:
  flutter_lints: ^2.0.2
  test: ^1.24.3

dependency_overrides:
  meta: ^1.12.0

  
  