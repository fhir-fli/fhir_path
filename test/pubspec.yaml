name: fhir_path_test
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  fhir_primitives: ^0.1.0
  fhir: ^0.12.0
  fhir_path:
    path:
      ../.
  flutter:
    sdk: flutter
  ucum: ^0.4.0

dev_dependencies:
  flutter_lints: ^2.0.2
  test: ^1.24.3

dependency_overrides:
  meta: ^1.12.0

  
  